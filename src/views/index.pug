<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        style(type="text/css").
            *{
                padding:0;
                margin:0;
                box-sizing:border-box;
            }
            .wrapper {
                background: #0182FC;
                height:100vh;
                display:flex; 
                justify-content:center; 
                align-items:center;
            }
            .input{
                padding:15px;
                width:800px;
                height:100px;
                font-size: 3rem;
                color:gray;
                border:0;
                outline:0;
                border-radius:100px;
            }
            .button{

                padding:15px 10px;
                width:400px;
                height:100px;
                font-size: 3rem;
                background: #006CD2;
                border:0;
                outline:0;
                border-radius:100px;
                margin-top:40px;
                color:#fff;
                cursor:pointer;
            }
            .form{
                display:flex;
                align-items:center;
                flex-direction:column;
            }
            .title{
                position:absolute;
                top:10%;
                left:50%;
                transform: translate(-50%,-10%);
                font-family:sans-serif;
                color:#fff;
                font-size: 4rem;
            }
            body{
                position:relative;
            }

    body
        h1.title ADD A TASK
        div.wrapper
            form(action="/tasks", method="post").form
                input(autocomplete ="off" type="text" name="text").input
                button(type="submit").button ADD