<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        style(type="text/css").
            *{
                padding:0;
                margin:0;
                box-sizing:border-box;
                font-family:sans-serif;
            }
            .wrapper {
                background: #0182FC;
                height:100vh;
                display:flex; 
                justify-content:center; 
                align-items:center;
            }
            .input{
                padding:15px 10px;
                width:800px;
                height:100px;
                font-size: 3rem;
                color:gray;
                border:0;
                outline:0;
                border-radius:100px;
            }
            .button{

                padding:15px 10px;
                width:400px;
                height:100px;
                font-size: 3rem;
                background: #006CD2;
                border:0;
                outline:0;
                border-radius:100px;
                margin-top:40px;
                color:#fff;
                cursor:pointer;
            }
            .form{
                display:flex;
                align-items:center;
                flex-direction:column;
            }
            .task-wrapper{
                display:flex;
                flex-direction:column;
                list-style:none;
                width:40%;
            }
            .task-item {
                display:flex;
                justify-content:space-between;
                color:#fff;
                font-size:2rem;
                background:#006CD2;
                margin-bottom:10px;
                padding:10px;
                cursor:pointer;
                border-radius:10px;
                font-family:sans-serif;
            }
            .task-item-info{
                display:flex;
            }
            .task-index{
                margin-right:20px;
            }
            .delete-form{
                display:flex;
                //- background:red;
            }
            .delete-form button {
                //- background: #006CD2;
                background:red;
                padding:10px;
                border-radius:10px;
                outline:none;
                border:none;
                color:#fff;
                font-weight:bold;
                font-size:1.2rem;
                cursor:pointer;
            }
            body {
                position:relative; 
            }
            .title {
                color:#fff;
                position: absolute;
                font-size:3rem;
                left:50%;
                top:10%;
                transform:translate(-50%,-10%);
            }

    body
        h1.title YOUR TASKS
        div.wrapper
            ul.task-wrapper
                each task,index in tasks
                    li.task-item
                        div.task-item-info 
                            p.task-index #{index} 
                            p.task-title #{task.title}
                        form.delete-form(action="/tasks/remove/"+task._id+"", method="post")
                            button(type="submit") BORRAR

                        

                    //- != '<div>'+index+' '+task.text+ '</div>' + '<form action="/tasks/"><a href="/tasks/">BORRAR</a></form>'
            //- h1 LIST ALL TASKS